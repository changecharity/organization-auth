import {createMuiTheme} from '@material-ui/core'

const theme = createMuiTheme({
    palette: {
      main: '#00aee5',
      sec: "#576574"
    },
    typography: {
      fontFamily: 'Roboto',
      logoFont: "GochiHand"
    }
  });

  export default theme